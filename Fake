<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>OMVERSE — AI Studio (Replica)</title>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&family=Orbitron:wght@500;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg-0: #05060a;
      --bg-1: #071122;
      --panel: rgba(255,255,255,0.03);
      --glass: rgba(255,255,255,0.02);
      --accent-cyan: #00f0ff;
      --accent-purple: #7b5cff;
      --muted: rgba(255,255,255,0.35);
      --success: #59ffa3;
      --danger: #ff6b6b;
      --glass-border: rgba(255,255,255,0.06);
      --blur: 18px;
      --radius-lg: 14px;
      --radius-sm: 10px;
      --shadow-neon: 0 6px 30px rgba(123,92,255,0.08), 0 2px 8px rgba(0,240,255,0.04);
    }

    /* Global */
    * { box-sizing: border-box; }
    html,body { height:100%; margin:0; font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; color:#e6f7ff; background: radial-gradient(1200px 800px at 10% 10%, rgba(0,240,255,0.03), transparent 6%), radial-gradient(1000px 700px at 90% 90%, rgba(123,92,255,0.02), transparent 10%), linear-gradient(180deg,var(--bg-0),var(--bg-1)); -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale; -webkit-tap-highlight-color: transparent;}
    a { color:inherit; text-decoration:none; }

    /* Layout */
    .app {
      display: grid;
      grid-template-columns: 88px 1fr 380px;
      gap: 20px;
      padding: 28px;
      height:100vh;
      align-items:stretch;
    }

    /* Sidebar (left) */
    .sidebar {
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border: 1px solid var(--glass-border);
      border-radius: 16px;
      padding: 18px 10px;
      display:flex;
      flex-direction:column;
      align-items:center;
      gap: 18px;
      position:relative;
      box-shadow: var(--shadow-neon);
      backdrop-filter: blur(var(--blur));
      min-height:0;
    }

    .logo-wrap{
      display:flex; align-items:center; gap:10px; flex-direction:column;
      width:100%;
    }

    .omi-badge{
      width:56px; height:56px; display:flex; align-items:center; justify-content:center;
      border-radius:12px;
      background: linear-gradient(135deg, rgba(0,240,255,0.12), rgba(123,92,255,0.12));
      border:1px solid rgba(255,255,255,0.06);
      box-shadow: 0 6px 20px rgba(123,92,255,0.06), inset 0 1px 0 rgba(255,255,255,0.03);
    }
    .logo-label { font-family:Orbitron, Inter; font-weight:700; letter-spacing:0.6px; font-size:9px; color:var(--accent-cyan); text-transform:uppercase; opacity:0.95; }

    .modes {
      display:flex; flex-direction:column; gap:12px; align-items:center; width:100%;
    }

    .mode-btn {
      width:56px; height:56px; border-radius:12px; display:flex; align-items:center; justify-content:center; background:linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.02));
      border:1px solid var(--glass-border); color:var(--muted); cursor:pointer; transition: all 220ms ease;
      position:relative; overflow:visible;
    }
    .mode-btn.active { color:#fff; box-shadow:0 6px 18px rgba(0,240,255,0.06), 0 0 30px rgba(123,92,255,0.05); transform: translateY(-4px); border:1px solid rgba(0,240,255,0.18); }
    .mode-btn svg { width:22px; height:22px; opacity:0.95; filter: drop-shadow(0 6px 18px rgba(0,240,255,0.06)); }

    .sidebar-footer { margin-top:auto; width:100%; display:flex; flex-direction:column; gap:8px; align-items:center; }

    /* Main (center) */
    .main {
      display:flex; flex-direction:column; gap:18px; min-width:0;
      backdrop-filter: blur(6px);
    }
    .topbar {
      display:flex; align-items:center; justify-content:space-between; gap:12px;
    }

    .welcome-card {
      background: linear-gradient(135deg, rgba(255,255,255,0.02), rgba(255,255,255,0.015));
      border:1px solid var(--glass-border);
      border-radius: var(--radius-lg);
      padding: 18px;
      display:flex; gap:18px; align-items:center; box-shadow: var(--shadow-neon);
    }
    .welcome-left { display:flex; gap:14px; align-items:center; min-width:0; }
    .omi-avatar {
      width:84px; height:84px; border-radius:14px; display:flex; align-items:center; justify-content:center;
      background: radial-gradient(circle at 30% 20%, rgba(0,240,255,0.12), transparent 20%), linear-gradient(180deg, rgba(123,92,255,0.06), rgba(0,0,0,0.02));
      border:1px solid rgba(255,255,255,0.04); box-shadow: 0 12px 40px rgba(0,0,0,0.5), inset 0 2px 10px rgba(255,255,255,0.02);
    }
    .welcome-title { font-family:Orbitron, Inter; font-size:20px; font-weight:700; color:#e8fbff; margin:0; }
    .welcome-sub { font-size:13px; color:var(--muted); margin-top:4px; }

    .mode-toggles { margin-left:auto; display:flex; gap:10px; align-items:center; }

    .pill {
      padding:8px 12px; border-radius:12px; background: linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border:1px solid var(--glass-border); color:var(--muted); font-weight:600; cursor:pointer; font-size:13px;
    }
    .pill.active { color:#fff; border:1px solid rgba(0,240,255,0.14); box-shadow: 0 6px 20px rgba(0,240,255,0.04); }

    /* Chat area */
    .chat-panel {
      flex:1; overflow:auto; padding:18px; border-radius:12px; background: linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.0));
      border:1px solid var(--glass-border); display:flex; flex-direction:column; gap:14px;
      min-height:0;
    }

    .chat-inner {
      display:flex; flex-direction:column; gap:14px; width:100%;
    }

    .msg {
      max-width:72%;
      padding:12px 14px;
      border-radius:12px;
      display:inline-block;
      line-height:1.25;
      font-size:14px;
      animation: bubbleIn .28s ease both;
      box-shadow: 0 6px 18px rgba(0,0,0,0.45);
      position:relative;
    }
    @keyframes bubbleIn {
      from { transform: translateY(8px) scale(0.98); opacity:0; }
      to { transform: translateY(0) scale(1); opacity:1; }
    }

    .msg.bot {
      align-self:flex-start;
      background: linear-gradient(180deg, rgba(0,240,255,0.06), rgba(123,92,255,0.03));
      border: 1px solid rgba(0,240,255,0.12);
      color: #e8fbff;
      box-shadow: 0 10px 30px rgba(0,240,255,0.03);
    }
    .msg.user {
      align-self:flex-end;
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border:1px solid rgba(255,255,255,0.04);
      color:#dff9ff;
      text-align:right;
    }

    .meta { font-size:11px; color:var(--muted); margin-top:8px; display:flex; gap:8px; align-items:center; justify-content:flex-start; }
    .msg.user .meta { justify-content:flex-end; }

    .quick-actions {
      display:flex; gap:10px; flex-wrap:wrap;
    }
    .quick {
      padding:8px 12px; border-radius:10px; background:rgba(255,255,255,0.02); border:1px solid var(--glass-border);
      color:var(--muted); cursor:pointer; font-size:13px; transition:all 180ms ease;
    }
    .quick:hover { transform:translateY(-4px); color:#fff; border:1px solid rgba(0,240,255,0.12); }

    /* Right panel (assistant controls / history) */
    .right {
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius: 16px; padding:18px; border:1px solid var(--glass-border); display:flex; flex-direction:column; gap:12px;
      min-height:0;
    }
    .section-title { font-weight:700; color:#e9fbff; font-size:13px; display:flex; align-items:center; gap:8px; }
    .history-list { display:flex; flex-direction:column; gap:8px; overflow:auto; padding-right:6px; }
    .history-item { padding:12px; border-radius:10px; background:transparent; border:1px solid rgba(255,255,255,0.02); color:var(--muted); cursor:pointer; display:flex; align-items:center; gap:10px; }
    .history-item:hover { background: rgba(255,255,255,0.01); color:#fff; transform:translateX(4px); }

    /* Input area */
    .composer {
      height:78px; border-radius:14px; padding:12px; display:flex; gap:12px; align-items:center;
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border:1px solid var(--glass-border);
      position:fixed; left: calc(88px + 28px); right: calc(380px + 28px); bottom: 28px; transition: all 200ms ease;
      backdrop-filter: blur(8px);
    }

    .composer .left {
      display:flex; gap:10px; align-items:center; min-width:0; flex:1;
    }

    .input {
      flex:1; background:transparent; border:none; outline:none; color:#e8fbff; font-size:15px;
      padding:12px 14px; border-radius:10px; border:1px dashed rgba(255,255,255,0.03);
    }
    .tool-btn {
      width:46px; height:46px; border-radius:10px; display:inline-flex; align-items:center; justify-content:center;
      background: linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.02));
      border:1px solid var(--glass-border); cursor:pointer; color:var(--muted);
    }
    .send-btn {
      min-width:64px; height:46px; padding:0 14px; display:inline-flex; gap:8px; align-items:center; justify-content:center; border-radius:10px;
      background: linear-gradient(90deg, rgba(0,240,255,0.14), rgba(123,92,255,0.12)); border: none; color:#01121a; font-weight:700; cursor:pointer;
      box-shadow: 0 8px 30px rgba(0,240,255,0.10);
    }

    /* Mobile / responsive */
    @media (max-width:1000px){
      .app { grid-template-columns: 72px 1fr; padding:16px; grid-auto-rows:auto; }
      .composer { left: calc(72px + 16px); right: 16px; bottom: 16px; height:72px; border-radius:12px; }
      .right { display:none; }
      .app { grid-template-columns: 72px 1fr; }
      .sidebar { padding:12px 8px; gap:12px; }
      .logo-wrap { gap:6px; }
      .welcome-title { font-size:16px; }
    }
    @media (max-width:640px){
      .app { grid-template-columns: 1fr; grid-template-rows: auto 1fr; padding:12px; }
      .sidebar { position:fixed; left:12px; top:12px; width:64px; z-index:80; }
      .composer { left: 12px; right:12px; bottom: 12px; }
      .welcome-card { flex-direction:column; align-items:flex-start; gap:12px; }
    }

    /* little utilities */
    .muted { color:var(--muted); font-size:13px; }
    .small { font-size:12px; color:var(--muted); }

    /* typing indicator */
    .typing {
      display:inline-flex; gap:6px; align-items:center;
    }
    .dot { width:7px; height:7px; background:rgba(255,255,255,0.9); border-radius:50%; opacity:0.16; animation: bounce 1s infinite; }
    .dot:nth-child(2) { animation-delay:0.12s; }
    .dot:nth-child(3) { animation-delay:0.24s; }
    @keyframes bounce {
      0% { transform: translateY(0); opacity:0.16; }
      40% { transform: translateY(-6px); opacity:1; }
      80% { transform: translateY(0); opacity:0.16; }
    }

  </style>

</head>
<body>
  <div class="app" id="app">
    <!-- LEFT SIDEBAR -->
    <aside class="sidebar" aria-label="Side navigation">
      <div class="logo-wrap">
        <div class="omi-badge" id="logo">
          <!-- Stylized OMI avatar SVG (unique vector, not an external image) -->
          <svg viewBox="0 0 64 64" width="46" height="46" fill="none" aria-hidden="true">
            <defs>
              <radialGradient id="g1" cx="30%" cy="24%">
                <stop offset="0" stop-color="#00f0ff" stop-opacity="0.95"></stop>
                <stop offset="1" stop-color="#7b5cff" stop-opacity="0.6"></stop>
              </radialGradient>
            </defs>
            <rect x="2" y="2" width="60" height="60" rx="12" fill="url(#g1)"></rect>
            <g transform="translate(12,12)" fill="#00121a">
              <path d="M20 2c-3.9 0-7 3.5-7 8s3.1 8 7 8c3.9 0 7-3.5 7-8s-3.1-8-7-8zm0 2c2.2 0 4 2.2 4 6s-1.8 6-4 6-4-2.2-4-6 1.8-6 4-6z"></path>
              <path d="M6 30c0-6.6 6.6-12 14-12s14 5.4 14 12v2H6v-2z" opacity="0.18"></path>
            </g>
          </svg>
        </div>
        <div class="logo-label">OMI STUDIO</div>
      </div>

      <div class="modes" role="tablist" aria-orientation="vertical">
        <button class="mode-btn active" data-mode="assistant" title="Assistant" aria-pressed="true">
          <svg viewBox="0 0 24 24"><path fill="currentColor" d="M12 2L2 7v6c0 5 6 9 10 9s10-4 10-9V7L12 2z"></path></svg>
        </button>
        <button class="mode-btn" data-mode="studio" title="Studio" aria-pressed="false">
          <svg viewBox="0 0 24 24"><path fill="currentColor" d="M3 7h18v10H3V7zm2 2v6h14V9H5z"></path></svg>
        </button>
        <button class="mode-btn" data-mode="explore" title="Explore" aria-pressed="false">
          <svg viewBox="0 0 24 24"><path fill="currentColor" d="M12 2l3.1 6.9L22 12l-6.9 3.1L12 22l-3.1-6.9L2 12l6.9-3.1L12 2z"></path></svg>
        </button>
      </div>

      <div class="sidebar-footer">
        <div class="small muted">Version 1.0 · Prototype</div>
        <button class="mode-btn" id="collapse" title="Collapse">
          <svg viewBox="0 0 24 24"><path fill="currentColor" d="M6 12h12v2H6z"/></svg>
        </button>
      </div>
    </aside>

    <!-- CENTER -->
    <main class="main">
      <div class="topbar">
        <div style="display:flex;gap:12px;align-items:center;">
          <div class="welcome-card" aria-live="polite">
            <div class="welcome-left">
              <div class="omi-avatar" aria-hidden="true">
                <svg viewBox="0 0 72 72" width="64" height="64">
                  <defs>
                    <linearGradient id="lg1" x1="0" x2="1">
                      <stop offset="0" stop-color="#00f0ff"></stop>
                      <stop offset="1" stop-color="#7b5cff"></stop>
                    </linearGradient>
                  </defs>
                  <rect x="4" y="4" width="64" height="64" rx="12" fill="url(#lg1)" opacity="0.18"></rect>
                  <g transform="translate(10,10)" fill="#e6fbff">
                    <path d="M26 4c-6.6 0-12 5.4-12 12s5.4 12 12 12 12-5.4 12-12S32.6 4 26 4z" opacity="0.9"></path>
                    <circle cx="26" cy="16" r="4" fill="#00121a"></circle>
                  </g>
                </svg>
              </div>
              <div style="min-width:0;">
                <div class="welcome-title">Welcome to OMVERSE</div>
                <div class="welcome-sub">Your AI creative studio — talk to OMI, create, or explore templates.</div>
              </div>
            </div>

            <div class="mode-toggles">
              <button class="pill active" data-mode-pill="chat">Chat</button>
              <button class="pill" data-mode-pill="create">Create</button>
              <button class="pill" data-mode-pill="assist">Assist</button>
            </div>
          </div>

          <div style="display:flex; align-items:center; gap:8px;">
            <div class="muted small">Model:</div>
            <div class="pill">OMI-X v2</div>
          </div>
        </div>

        <div style="display:flex; align-items:center; gap:12px;">
          <div class="quick-actions" role="toolbar" aria-label="Quick actions">
            <button class="quick" data-quick="summarize">Summarize</button>
            <button class="quick" data-quick="rewrite">Rewrite</button>
            <button class="quick" data-quick="idea">Generate Idea</button>
          </div>
        </div>
      </div>

      <section class="chat-panel" id="chatPanel" aria-label="Chat panel">
        <div class="chat-inner" id="chatInner">
          <!-- sample messages -->
          <div class="msg bot">
            Hey — I'm OMI. How can I help you create today?
            <div class="meta"><span class="small">OMI · just now</span></div>
          </div>

          <div class="msg user">
            I want a futuristic brand concept for "Neon Harbor".
            <div class="meta"><span class="small">You · 1m</span></div>
          </div>

          <div class="msg bot">
            Great — here's a short concept: <strong>Neon Harbor</strong> — a nocturnal tech brand blending maritime imagery with cyberpunk neon. Would you like logo ideas, color palettes, or a tagline?
            <div class="meta"><span class="small">OMI · 45s</span></div>
          </div>
        </div>
      </section>
    </main>

    <!-- RIGHT COLUMN -->
    <aside class="right" aria-label="Assistant controls">
      <div class="section-title">Quick Templates</div>
      <div class="history-list">
        <div class="history-item" data-template="Write a friendly product description for: ">
          Product Description
        </div>
        <div class="history-item" data-template="Create social captions for: ">
          Social Captions
        </div>
        <div class="history-item" data-template="Generate 5 name ideas for: ">
          Name Ideas
        </div>
        <div class="history-item" data-template="Create an onboarding email for: ">
          Onboarding Email
        </div>
      </div>

      <div style="height:8px"></div>

      <div class="section-title">Conversation Tools</div>
      <div style="display:flex;gap:10px;flex-wrap:wrap;">
        <button class="quick" id="regenerate">Regenerate</button>
        <button class="quick" id="clearChat">Clear</button>
        <button class="quick" id="exportChat">Export</button>
      </div>

      <div style="height:8px"></div>

      <div class="section-title">Settings</div>
      <div style="display:flex;flex-direction:column;gap:8px;">
        <div class="small muted">Appearance</div>
        <div style="display:flex;gap:8px;">
          <button class="pill" data-accent="cyan">Cyan</button>
          <button class="pill" data-accent="purple">Purple</button>
        </div>
      </div>
    </aside>

    <!-- Composer (fixed) -->
    <div class="composer" role="region" aria-label="Message composer">
      <div class="left">
        <button class="tool-btn" id="attach" title="Attach">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor"><path d="M16.5 6.7L9.4 13.8a4 4 0 11-5.7-5.6l7.1-7.1a6 6 0 118.5 8.5L11 19.3"></path></svg>
        </button>

        <input id="inputBox" class="input" placeholder="Ask OMI... (try: 'Create a brand tagline for a cyberpunk cafe')" aria-label="Message input" />

        <button class="tool-btn" id="mic" title="Voice input">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="currentColor"><path d="M12 14a3 3 0 003-3V5a3 3 0 10-6 0v6a3 3 0 003 3z"></path><path d="M19 11v1a7 7 0 01-14 0v-1"></path></svg>
        </button>
      </div>

      <div style="display:flex;gap:12px;align-items:center">
        <button class="send-btn" id="sendBtn" title="Send message">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="#01121a" style="transform:rotate(-20deg)"><path d="M2 21l21-9L2 3v7l15 2-15 2v7z"></path></svg>
          Send
        </button>
      </div>
    </div>

  </div>

  <script>
    // Minimal interactive behavior to emulate OMVERSE studio UX:
    (function(){
      const app = document.getElementById('app');
      const chatInner = document.getElementById('chatInner');
      const inputBox = document.getElementById('inputBox');
      const sendBtn = document.getElementById('sendBtn');
      const quicks = document.querySelectorAll('[data-quick]');
      const historyTemplates = document.querySelectorAll('.history-item');
      const modeButtons = document.querySelectorAll('.mode-btn');
      const pills = document.querySelectorAll('.pill[data-accent], .pill[data-mode-pill]');
      const accentButtons = document.querySelectorAll('[data-accent]');
      const collapseBtn = document.getElementById('collapse');

      // Utility to add message
      function addMessage(text, who='bot', timeLabel='just now', opts={}) {
        const node = document.createElement('div');
        node.className = 'msg ' + (who === 'user' ? 'user' : 'bot');
        node.innerHTML = sanitize(text) + '<div class="meta"><span class="small">' + (who === 'user' ? 'You' : 'OMI') + ' · ' + timeLabel + '</span></div>';
        chatInner.appendChild(node);
        // scroll
        chatInner.parentElement.scrollTop = chatInner.parentElement.scrollHeight;
      }

      // sanitize minimal
      function sanitize(s){
        const div = document.createElement('div');
        div.textContent = s;
        return div.innerHTML.replace(/\\n/g,'<br/>');
      }

      // Simulate a typing indicator
      function showTyping() {
        const t = document.createElement('div');
        t.className = 'msg bot';
        t.id = 'typingIndicator';
        t.innerHTML = '<div class="typing"><span class="dot"></span><span class="dot"></span><span class="dot"></span></div><div class="meta"><span class="small">OMI · typing</span></div>';
        chatInner.appendChild(t);
        chatInner.parentElement.scrollTop = chatInner.parentElement.scrollHeight;
      }
      function removeTyping() {
        const t = document.getElementById('typingIndicator');
        if (t) t.remove();
      }

      // send event
      function sendMessage(){
        const text = inputBox.value.trim();
        if (!text) return;
        addMessage(text, 'user', 'now');
        inputBox.value = '';
        // show typing
        showTyping();
        // simulate network delay
        setTimeout(() => {
          removeTyping();
          // generate reply (simple echo + small enhancement)
          const reply = generateReply(text);
          addMessage(reply, 'bot', 'just now');
        }, 900 + Math.random()*900);
      }

      function generateReply(prompt){
        // short pseudo-AI reply patterns to emulate OMI behavior
        const lower = prompt.toLowerCase();
        if (lower.includes('tagline') || lower.includes('slogan')) {
          return 'Tagline ideas: "Glow beyond horizon", "Where neon meets narrative", "Harbors of tomorrow."';
        }
        if (lower.includes('color') || lower.includes('palette')) {
          return 'Suggested palette: Deep #071122 background, Neon Cyan #00f0ff, Ultraviolet #7b5cff, Soft Coral #ff8aa3.';
        }
        if (lower.includes('logo') || lower.includes('icon')) {
          return 'Logo idea: a stylized orbital beacon merging an anchor shape with a glowing ring — logotype in Orbitron.';
        }
        if (lower.includes('summarize')) {
          return 'Summary: Neon Harbor is a nocturnal tech brand that blends maritime motifs with cyberpunk neon; ideal for nightlife, F&B, and immersive tech experiences.';
        }
        // fallback
        return "I can help with that — do you want variations, a short description, or a creative direction?";
      }

      // Quick action handlers
      quicks.forEach(q => q.addEventListener('click', (e)=>{
        const qv = e.currentTarget.dataset.quick;
        if (qv === 'summarize') {
          inputBox.value = 'Summarize the following text: ';
        } else if (qv === 'rewrite') {
          inputBox.value = 'Rewrite this to be more concise: ';
        } else if (qv === 'idea') {
          inputBox.value = 'Generate 10 ideas for: ';
        }
        inputBox.focus();
      }));

      // history templates
      historyTemplates.forEach(it => it.addEventListener('click', ()=>{
        inputBox.value = it.dataset.template;
        inputBox.focus();
      }));

      // send on click or Enter
      sendBtn.addEventListener('click', sendMessage);
      inputBox.addEventListener('keydown', function(e){
        if (e.key === 'Enter' && !e.shiftKey) {
          e.preventDefault();
          sendMessage();
        }
      });

      // mode buttons toggle
      modeButtons.forEach(btn => btn.addEventListener('click', ()=>{
        modeButtons.forEach(b=> { b.classList.remove('active'); b.setAttribute('aria-pressed','false'); });
        btn.classList.add('active');
        btn.setAttribute('aria-pressed','true');
        // small visual feedback: change main accent
        const mode = btn.dataset.mode;
        if (mode === 'studio') {
          document.documentElement.style.setProperty('--accent-cyan', '#8af1ff');
          document.documentElement.style.setProperty('--accent-purple', '#a07bff');
        } else if (mode === 'explore') {
          document.documentElement.style.setProperty('--accent-cyan', '#00ffd0');
          document.documentElement.style.setProperty('--accent-purple', '#ff7bdc');
        } else {
          document.documentElement.style.setProperty('--accent-cyan', '#00f0ff');
          document.documentElement.style.setProperty('--accent-purple', '#7b5cff');
        }
      }));

      // accent pill buttons
      accentButtons.forEach(b => b.addEventListener('click', ()=>{
        accentButtons.forEach(x => x.classList.remove('active'));
        b.classList.add('active');
        const c = b.dataset.accent;
        if (c === 'cyan') {
          document.documentElement.style.setProperty('--accent-cyan', '#00f0ff');
        } else if (c === 'purple') {
          document.documentElement.style.setProperty('--accent-cyan', '#b38bff');
          document.documentElement.style.setProperty('--accent-purple', '#7b5cff');
        }
      }));

      // collapse sidebar for small screens
      collapseBtn.addEventListener('click', ()=>{
        const sb = document.querySelector('.sidebar');
        sb.classList.toggle('collapsed');
        if (sb.classList.contains('collapsed')) {
          sb.style.transform = 'translateX(-52px)';
          sb.style.opacity = '0.9';
        } else {
          sb.style.transform = '';
          sb.style.opacity = '';
        }
      });

      // Regenerate / clear
      document.getElementById('regenerate').addEventListener('click', ()=>{
        // take last user message and regenerate
        const items = chatInner.querySelectorAll('.msg.user');
        const last = items[items.length - 1];
        if (last) {
          const text = last.firstChild.textContent || last.textContent;
          showTyping();
          setTimeout(()=> {
            removeTyping();
            addMessage(generateReply(text.trim()), 'bot', 'just now');
          }, 600 + Math.random()*800);
        } else {
          addMessage("Start the conversation by typing a prompt.", 'bot', 'just now');
        }
      });
      document.getElementById('clearChat').addEventListener('click', ()=>{
        chatInner.innerHTML = '';
        addMessage("Conversation cleared. I'm ready when you are.", 'bot', 'just now');
      });
      document.getElementById('exportChat').addEventListener('click', ()=>{
        const text = Array.from(chatInner.querySelectorAll('.msg')).map(m => {
          const who = m.classList.contains('user') ? 'You' : 'OMI';
          const content = m.childNodes[0].textContent || m.textContent;
          return who + ': ' + content.trim();
        }).join('\\n\\n');
        const blob = new Blob([text], {type:'text/plain'});
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url; a.download = 'omverse-conversation.txt';
        a.click();
        URL.revokeObjectURL(url);
      });

      // Preload: keep scroll at bottom
      setTimeout(()=> {
        chatInner.parentElement.scrollTop = chatInner.parentElement.scrollHeight;
      },100);

      // Accessibility: focus composer on load
      inputBox.focus();

    })();
  </script>
</body>
                    </html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>OMVERSE - Omi AI Companion</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }

        :root {
            --primary: #00d4ff;
            --primary-dark: #0099cc;
            --secondary: #ff006e;
            --accent: #8000ff;
            --dark-bg: #0a0e27;
            --darker-bg: #050812;
            --card-bg: #1a1f3a;
            --text-primary: #ffffff;
            --text-secondary: #b0b5c8;
            --border-color: #2a3050;
            --success: #00ff88;
            --warning: #ffaa00;
            --danger: #ff3366;
        }

        html, body {
            width: 100%;
            height: 100%;
            overflow: hidden;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, var(--dark-bg) 0%, var(--darker-bg) 100%);
            color: var(--text-primary);
        }

        /* Scrollbar styling */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: var(--primary);
            border-radius: 3px;
        }

        .app-container {
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: linear-gradient(135deg, var(--dark-bg) 0%, var(--darker-bg) 100%);
            position: relative;
            overflow: hidden;
        }

        /* Header */
        .header {
            background: linear-gradient(180deg, rgba(26, 31, 58, 0.95) 0%, rgba(10, 14, 39, 0.8) 100%);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--border-color);
            padding: 12px 16px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
            z-index: 100;
            min-height: 56px;
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 8px;
            flex: 1;
        }

        .omi-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            font-weight: bold;
            box-shadow: 0 0 15px rgba(0, 212, 255, 0.5);
            animation: pulse-glow 2s ease-in-out infinite;
        }

        @keyframes pulse-glow {
            0%, 100% { box-shadow: 0 0 15px rgba(0, 212, 255, 0.5); }
            50% { box-shadow: 0 0 25px rgba(0, 212, 255, 0.8); }
        }

        .header-title {
            display: flex;
            flex-direction: column;
        }

        .app-name {
            font-size: 16px;
            font-weight: 700;
            color: var(--primary);
            letter-spacing: 1px;
        }

        .status-text {
            font-size: 11px;
            color: var(--success);
            text-transform: uppercase;
        }

        .header-right {
            display: flex;
            gap: 8px;
        }

        .header-btn {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            border: none;
            background: rgba(0, 212, 255, 0.1);
            color: var(--primary);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            transition: all 0.3s ease;
            position: relative;
        }

        .header-btn:active {
            background: rgba(0, 212, 255, 0.25);
            transform: scale(0.95);
        }

        /* Main Content Area */
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        /* Action Buttons */
        .action-buttons {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 8px;
            padding: 12px 12px 0;
            max-height: 100px;
            overflow-y: auto;
        }

        .action-btn {
            padding: 10px 12px;
            border-radius: 10px;
            border: 1px solid var(--border-color);
            background: rgba(26, 31, 58, 0.6);
            color: var(--text-primary);
            cursor: pointer;
            font-size: 12px;
            font-weight: 600;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 4px;
            transition: all 0.3s ease;
            white-space: nowrap;
        }

        .action-btn:active {
            transform: translateY(-2px);
            background: rgba(0, 212, 255, 0.15);
            border-color: var(--primary);
            box-shadow: 0 0 10px rgba(0, 212, 255, 0.3);
        }

        .action-btn-icon {
            font-size: 18px;
        }

        /* Chat Area */
        .chat-area {
            flex: 1;
            overflow-y: auto;
            padding: 12px 12px;
            display: flex;
            flex-direction: column;
            gap: 12px;
            scroll-behavior: smooth;
        }

        .message {
            display: flex;
            animation: fadeIn 0.3s ease;
            word-wrap: break-word;
            overflow-wrap: break-word;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .message.user {
            justify-content: flex-end;
        }

        .message-content {
            max-width: 85%;
            padding: 10px 14px;
            border-radius: 12px;
            word-break: break-word;
            line-height: 1.4;
            font-size: 14px;
        }

        .user .message-content {
            background: linear-gradient(135deg, var(--primary), var(--accent));
            border-bottom-right-radius: 4px;
            color: var(--dark-bg);
            font-weight: 500;
        }

        .bot .message-content {
            background: rgba(26, 31, 58, 0.8);
            border: 1px solid var(--border-color);
            border-bottom-left-radius: 4px;
            color: var(--text-secondary);
        }

        .message-time {
            font-size: 11px;
            color: var(--text-secondary);
            margin-top: 4px;
            margin-left: 8px;
            margin-right: 8px;
        }

        .user .message-time {
            text-align: right;
            margin-left: auto;
            order: -1;
            margin-right: 0;
        }

        /* Input Area */
        .input-area {
            padding: 12px;
            border-top: 1px solid var(--border-color);
            background: linear-gradient(180deg, rgba(10, 14, 39, 0.5) 0%, rgba(5, 8, 18, 0.8) 100%);
            backdrop-filter: blur(10px);
            display: flex;
            gap: 8px;
            align-items: flex-end;
            max-height: 120px;
        }

        .input-wrapper {
            flex: 1;
            display: flex;
            gap: 6px;
            align-items: flex-end;
        }

        .chat-input {
            flex: 1;
            background: rgba(26, 31, 58, 0.6);
            border: 1px solid var(--border-color);
            border-radius: 10px;
            padding: 10px 12px;
            color: var(--text-primary);
            font-size: 14px;
            max-height: 100px;
            resize: none;
            font-family: inherit;
            transition: all 0.3s ease;
        }

        .chat-input::placeholder {
            color: var(--text-secondary);
        }

        .chat-input:focus {
            outline: none;
            border-color: var(--primary);
            background: rgba(26, 31, 58, 0.9);
            box-shadow: 0 0 15px rgba(0, 212, 255, 0.2);
        }

        .send-btn {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            border: none;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            color: var(--dark-bg);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            font-weight: bold;
            transition: all 0.3s ease;
            flex-shrink: 0;
        }

        .send-btn:active {
            transform: scale(0.95);
            box-shadow: 0 0 20px rgba(0, 212, 255, 0.5);
        }

        /* Modals */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(5px);
            z-index: 1000;
            animation: fadeIn 0.3s ease;
            overflow-y: auto;
            padding: 20px 0;
        }

        .modal.active {
            display: flex;
            justify-content: center;
            align-items: flex-start;
        }

        .modal-content {
            background: linear-gradient(135deg, var(--card-bg) 0%, rgba(26, 31, 58, 0.8) 100%);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            padding: 20px;
            width: 90%;
            max-width: 500px;
            margin-top: 60px;
            max-height: 80vh;
            overflow-y: auto;
            box-shadow: 0 20px 60px rgba(0, 212, 255, 0.2);
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 12px;
            border-bottom: 1px solid var(--border-color);
        }

        .modal-title {
            font-size: 18px;
            font-weight: 700;
            color: var(--primary);
        }

        .close-btn {
            background: none;
            border: none;
            color: var(--text-secondary);
            font-size: 24px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .close-btn:active {
            color: var(--primary);
            transform: rotate(90deg);
        }

        /* Settings Modal */
        .settings-group {
            margin-bottom: 20px;
        }

        .settings-label {
            font-size: 12px;
            color: var(--text-secondary);
            text-transform: uppercase;
            margin-bottom: 8px;
            display: block;
        }

        .toggle-switch {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px;
            background: rgba(26, 31, 58, 0.4);
            border-radius: 10px;
            border: 1px solid var(--border-color);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .toggle-switch:active {
            background: rgba(0, 212, 255, 0.1);
        }

        .switch {
            position: relative;
            width: 44px;
            height: 24px;
            background: rgba(100, 100, 120, 0.5);
            border-radius: 12px;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .switch input {
            display: none;
        }

        .switch input:checked + .slider {
            background: var(--primary);
            box-shadow: 0 0 10px rgba(0, 212, 255, 0.5);
        }

        .slider {
            position: absolute;
            top: 2px;
            left: 2px;
            width: 20px;
            height: 20px;
            background: var(--text-secondary);
            border-radius: 50%;
            transition: all 0.3s ease;
        }

        input:checked + .slider {
            left: 22px;
        }

        .setting-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px;
            background: rgba(26, 31, 58, 0.4);
            border-radius: 10px;
            border: 1px solid var(--border-color);
            margin-bottom: 8px;
        }

        .setting-label {
            font-size: 14px;
            color: var(--text-primary);
        }

        .setting-value {
            color: var(--primary);
            font-weight: 600;
        }

        /* Game/Quiz Modal */
        .quiz-question {
            font-size: 16px;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 16px;
            padding: 12px;
            background: rgba(0, 212, 255, 0.1);
            border-left: 3px solid var(--primary);
            border-radius: 8px;
        }

        .quiz-options {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .quiz-option {
            padding: 12px;
            background: rgba(26, 31, 58, 0.6);
            border: 1px solid var(--border-color);
            border-radius: 10px;
            color: var(--text-primary);
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 14px;
            text-align: left;
        }

        .quiz-option:active {
            background: rgba(0, 212, 255, 0.2);
            border-color: var(--primary);
            transform: translateX(4px);
        }

        .quiz-score {
            font-size: 24px;
            font-weight: 700;
            color: var(--success);
            text-align: center;
            padding: 20px;
            background: rgba(0, 255, 136, 0.1);
            border-radius: 12px;
            margin-bottom: 16px;
        }

        /* Roast Content */
        .roast-content {
            font-size: 16px;
            line-height: 1.6;
            color: var(--text-secondary);
            padding: 16px;
            background: rgba(255, 0, 110, 0.1);
            border-left: 3px solid var(--secondary);
            border-radius: 8px;
            margin-bottom: 16px;
        }

        .roast-btn {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, var(--secondary), #ff3366);
            border: none;
            border-radius: 10px;
            color: white;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 14px;
        }

        .roast-btn:active {
            transform: translateY(-2px);
            box-shadow: 0 0 20px rgba(255, 0, 110, 0.5);
        }

        /* Samosa Coins */
        .coins-display {
            font-size: 28px;
            font-weight: 700;
            color: var(--warning);
            text-align: center;
            padding: 20px;
            background: rgba(255, 170, 0, 0.1);
            border-radius: 12px;
            margin-bottom: 16px;
        }

        .coins-emoji {
            font-size: 32px;
            margin-right: 8px;
        }

        /* History Clear Button */
        .clear-history-btn {
            width: 100%;
            padding: 12px;
            background: rgba(255, 51, 102, 0.1);
            border: 1px solid var(--danger);
            border-radius: 10px;
            color: var(--danger);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 14px;
            margin-top: 12px;
        }

        .clear-history-btn:active {
            background: rgba(255, 51, 102, 0.2);
            transform: translateY(-2px);
        }

        /* Time Pass Content */
        .fact-card {
            padding: 16px;
            background: rgba(26, 31, 58, 0.6);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            margin-bottom: 12px;
            line-height: 1.6;
            font-size: 14px;
        }

        .time-pass-btn {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, var(--accent), #6600ff);
            border: none;
            border-radius: 10px;
            color: white;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 14px;
        }

        .time-pass-btn:active {
            transform: translateY(-2px);
            box-shadow: 0 0 20px rgba(128, 0, 255, 0.5);
        }

        /* Empty State */
        .empty-state {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100%;
            color: var(--text-secondary);
            gap: 12px;
        }

        .empty-state-icon {
            font-size: 48px;
            opacity: 0.5;
        }

        .empty-state-text {
            font-size: 14px;
            text-align: center;
        }

        /* Responsive Design */
        @media (min-width: 768px) {
            .modal-content {
                width: 70%;
                max-width: 600px;
            }

            .action-buttons {
                grid-template-columns: repeat(4, 1fr);
            }

            .message-content {
                max-width: 70%;
            }
        }

        @media (min-width: 1024px) {
            .modal-content {
                width: 60%;
                max-width: 700px;
            }

            .action-buttons {
                grid-template-columns: repeat(6, 1fr);
            }

            .message-content {
                max-width: 60%;
            }
        }

        /* Loading Spinner */
        .typing-indicator {
            display: flex;
            gap: 4px;
            padding: 10px 14px;
            background: rgba(26, 31, 58, 0.8);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            border-bottom-left-radius: 4px;
            width: fit-content;
        }

        .typing-dot {
            width: 8px;
            height: 8px;
            background: var(--primary);
            border-radius: 50%;
            animation: typing 1.4s infinite;
        }

        .typing-dot:nth-child(2) {
            animation-delay: 0.2s;
        }

        .typing-dot:nth-child(3) {
            animation-delay: 0.4s;
        }

        @keyframes typing {
            0%, 60%, 100% { opacity: 0.5; }
            30% { opacity: 1; }
        }

        /* Touch Feedback */
        .ripple {
            position: absolute;
            border-radius: 50%;
            background: rgba(0, 212, 255, 0.5);
            transform: scale(0);
            animation: ripple-animation 0.6s ease-out;
            pointer-events: none;
        }

        @keyframes ripple-animation {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }

        /* Notification */
        .notification {
            position: fixed;
            bottom: 20px;
            left: 20px;
            right: 20px;
            padding: 12px 16px;
            background: rgba(26, 31, 58, 0.95);
            border: 1px solid var(--primary);
            border-radius: 10px;
            color: var(--primary);
            font-size: 14px;
            animation: slideUp 0.3s ease;
            z-index: 2000;
            max-width: 300px;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="app-container">
        <!-- Header -->
        <div class="header">
            <div class="header-left">
                <div class="omi-avatar">Omi</div>
                <div class="header-title">
                    <span class="app-name">OMVERSE</span>
                    <span class="status-text">● Online</span>
                </div>
            </div>
            <div class="header-right">
                <button class="header-btn" id="settingsBtn" title="Settings">⚙️</button>
            </div>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <!-- Action Buttons -->
            <div class="action-buttons">
                <button class="action-btn" id="chatBtn">
                    <span class="action-btn-icon">💬</span>
                    <span>Chat</span>
                </button>
                <button class="action-btn" id="roastBtn">
                    <span class="action-btn-icon">🔥</span>
                    <span>Roast Me</span>
                </button>
                <button class="action-btn" id="quizBtn">
                    <span class="action-btn-icon">🎮</span>
                    <span>Quiz</span>
                </button>
                <button class="action-btn" id="coinsBtn">
                    <span class="action-btn-icon">🥟</span>
                    <span>Samosa Coins</span>
                </button>
                <button class="action-btn" id="timePassBtn">
                    <span class="action-btn-icon">⏰</span>
                    <span>Time Pass</span>
                </button>
                <button class="action-btn" id="historyBtn">
                    <span class="action-btn-icon">📜</span>
                    <span>History</span>
                </button>
            </div>

            <!-- Chat Area -->
            <div class="chat-area" id="chatArea">
                <div class="empty-state">
                    <div class="empty-state-icon">👋</div>
                    <div class="empty-state-text">Namaste! I'm Omi, your AI companion. Kya haal hai? 🎉</div>
                </div>
            </div>

            <!-- Input Area -->
            <div class="input-area">
                <div class="input-wrapper">
                    <textarea 
                        class="chat-input" 
                        id="chatInput" 
                        placeholder="Suno Omi... kuch bolna hai?" 
                        rows="1"
                    ></textarea>
                    <button class="send-btn" id="sendBtn">📤</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Settings Modal -->
    <div class="modal" id="settingsModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">⚙️ Settings</h2>
                <button class="close-btn" onclick="closeModal('settingsModal')">✕</button>
            </div>

            <div class="settings-group">
                <label class="settings-label">Notification</label>
                <label class="toggle-switch">
                    <span>Enable Notifications</span>
                    <label class="switch">
                        <input type="checkbox" id="notificationToggle" checked>
                        <span class="slider"></span>
                    </label>
                </label>
            </div>

            <div class="settings-group">
                <label class="settings-label">Sound</label>
                <label class="toggle-switch">
                    <span>Message Sound</span>
                    <label class="switch">
                        <input type="checkbox" id="soundToggle" checked>
                        <span class="slider"></span>
                    </label>
                </label>
            </div>

            <div class="settings-group">
                <label class="settings-label">Hinglish Level</label>
                <div class="setting-item">
                    <span class="setting-label">Omi's Personality</span>
                    <span class="setting-value" id="hinglishLevel">Strong</span>
                </div>
                <input type="range" id="hinglishSlider" min="1" max="3" value="2" style="width: 100%;">
            </div>

            <div class="settings-group">
                <label class="settings-label">About</label>
                <div class="setting-item">
                    <span class="setting-label">Version</span>
                    <span class="setting-value">1.0.0</span>
                </div>
            </div>

            <button class="clear-history-btn" id="clearHistoryBtn">🗑️ Clear All History</button>
        </div>
    </div>

    <!-- Roast Modal -->
    <div class="modal" id="roastModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">🔥 Roast Me!</h2>
                <button class="close-btn" onclick="closeModal('roastModal')">✕</button>
            </div>

            <div id="roastContent" class="roast-content">
                Click the button below to get roasted by Omi! 😈
            </div>

            <button class="roast-btn" id="roastMeBtn">Roast Me Now! 🔥</button>
        </div>
    </div>

    <!-- Quiz Modal -->
    <div class="modal" id="quizModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">🎮 Quiz Time</h2>
                <button class="close-btn" onclick="closeModal('quizModal')">✕</button>
            </div>

            <div id="quizContent">
                <button class="time-pass-btn" id="startQuizBtn">Start Quiz</button>
            </div>
        </div>
    </div>

    <!-- Coins Modal -->
    <div class="modal" id="coinsModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">🥟 Samosa Coins</h2>
                <button class="close-btn" onclick="closeModal('coinsModal')">✕</button>
            </div>

            <div class="coins-display">
                <span class="coins-emoji">🥟</span>
                <span id="coinsAmount">0</span>
            </div>

            <div style="color: var(--text-secondary); font-size: 14px; line-height: 1.6;">
                <p>Earn Samosa Coins by:</p>
                <ul style="margin-left: 20px; margin-top: 10px;">
                    <li>💬 Chatting with Omi</li>
                    <li>🎮 Playing games</li>
                    <li>🔥 Getting roasted</li>
                    <li>⏰ Daily logins</li>
                </ul>
            </div>

            <button class="time-pass-btn" style="margin-top: 16px;" id="claimCoinsBtn">Claim Daily Bonus</button>
        </div>
    </div>

    <!-- Time Pass Modal -->
    <div class="modal" id="timePassModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">⏰ Time Pass</h2>
                <button class="close-btn" onclick="closeModal('timePassModal')">✕</button>
            </div>

            <div id="timePassContent">
                <button class="time-pass-btn" id="getFactBtn">Get Interesting Fact</button>
            </div>
        </div>
    </div>

    <!-- History Modal -->
    <div class="modal" id="historyModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">📜 Chat History</h2>
                <button class="close-btn" onclick="closeModal('historyModal')">✕</button>
            </div>

            <div id="historyContent" style="max-height: 400px; overflow-y: auto;">
                <div class="empty-state" style="height: auto; padding: 20px;">
                    <div class="empty-state-text">No chat history yet</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // ==================== APP STATE ====================
        const appState = {
            chatHistory: [],
            samosaCoins: 0,
            quizScore: 0,
            hinglishLevel: 2,
            notifications: true,
            sound: true,
            lastDailyBonus: null
        };

        // ==================== HINGLISH PERSONALITY ====================
        const hinglishPersonalities = {
            1: {
                greeting: "Hola buddy! Kya chal raha hai?",
                responses: [
                    "Bilkul sahi baat hai bro! 💯",
                    "Ekdum mast! Teri soch bilkul same hai meri jaisi. 🔥",
                    "Haan haan, tu bilkul sahi samajh gaya! 👍",
                    "Yaar, tu bahut smart hai! 🧠",
                    "Tere pe bilkul asar nahi padta na! 😂"
                ]
            },
            2: {
                greeting: "Namaste! Omi teri savari ke liye ready! 🎉",
                responses: [
                    "Haan bhai, bilkul sahi baat! Teri intelligence kamaal hai! 🚀",
                    "Yaar tere se better koi nahi! Kya plan hai aaj? 💬",
                    "Oye, tu kitna talented hai! Pata chal gaya ab! 🎵",
                    "Bilkul jhakkas! Tere jaise intelligent aadmi mujhe kamyaab banana chahte hain! 😂",
                    "Itna smart hu tu! Agli baar NASA ko apply kar de! 🚀"
                ]
            },
            3: {
                greeting: "Arre bhaiyya, desh ka superhero aa gaya! 🦸",
                responses: [
                    "Oye, tu kaun sa rocket science solve kar raha hai? Bilkul ISRO level! 🚀🇮🇳",
                    "Yaar, teri soch se mujhe inspiration mil gaya! Aage bado bro! 💪",
                    "Tera gyaan mujhe alag level pe le gaya! Ekdum paakka! 🔥",
                    "Bhai tu to Einstein ka great-great-grandson hai! 🧬",
                    "Suna hai Hollywood ne tere liye role tayyar kiya hai! 🎬"
                ]
            }
        };

        const roasts = [
            "Arre, tere andar itni creativity hai ki Netflix ne apni original series cancel kar di! 📺",
            "Tere style se to Shahrukh Khan ko jealousy ho jata hai! Thoda share kar bhai! 😂",
            "Teri knowledge itni badiya hai ki Google ne tujhe CEO banna ke liye call kiya! 🔥",
            "Tere jaise aadmi ko dekh kar meri inferiority complex trigger ho gaya! 😅",
            "Bhai tu to real life superhero ho! Teri superpowers se mujhe dar lag raha hai! 🦸‍♂️",
            "Teri wit itni sharp hai ki swords jealous ho gaye! ⚔️",
            "Tere personality se influencers unemployed ho gaye! 💅",
            "Tu itna funny hai ki comedians ko apna career change karna padega! 🎭",
            "Teri intelligence ka record world record book mein aa jana chahiye! 📖",
            "Oye, tu to absolute legend hai! Mujhe lagta hai tu bhagwan ka favorite creation hai! 🙏"
        ];

        const facts = [
            "जानते हो? Honey कभी खराब नहीं होता! 5000 साल पुरानी Honey अभी भी खाने के लिए अच्छी है! 🍯",
            "Did you know? एक शेर की दहाड़ 9 KM दूर से सुनाई दे सकती है! शायद तेरे पापा को उसी का डर है! 🦁",
            "अरे! Octopus के 3 दिल होते हैं! तो तेरे अंदर कितने हृदय होंगे अतितेरे लिए! 🐙",
            "Crazy fact: Bananas radioactive होते हैं! तो सावधान रहना, कहीं तू भी radiate न हो जाए! 🍌",
            "Jano na? एक दिन Venus पर एक साल से ज्यादा लंबा होता है Earth के दिन से! तो Venus पर तेरी छुट्टियां बहुत लंबी होंगी! 🪐",
            "मजेदार बात: पृथ्वी हर सेकंड 100 बार lightning से टकराई जाती है! और तू यहाँ शांति से बैठा है! ⚡",
            "Did you know? Dolphins को अपना नाम होता है! क्या तुम्हें अपना फोन नंबर भी याद है? 🐬",
            "Interesting: एक Giraffe की जीभ 20 inches लंबी होती है! क्या तुम्हारी चाटने की क्षमता भी इतनी है? 🦒",
            "Wow fact: Light को Sun से Earth तक आने में 8 मिनट लगते हैं! पर तेरा WhatsApp मैसेज आता ही नहीं! 💡",
            "Jano na? Cats 9 जिंदगियों के साथ पैदा होते हैं! तो तेरे पास कितनी जिंदगियां हैं जिंदगी को बर्बाद करने के लिए? 🐱"
        ];

        const quizQuestions = [
            {
                question: "भारत की राजधानी क्या है?",
                options: ["दिल्ली", "मुंबई", "कोलकाता", "चेन्नई"],
                correct: 0
            },
            {
                question: "एक साल में कितने महीने होते हैं?",
                options: ["10", "12", "13", "11"],
                correct: 1
            },
            {
                question: "पृथ्वी के चारों ओर कौन घूमता है?",
                options: ["चाँद", "सूरज", "तारे", "ग्रह"],
                correct: 0
            },
            {
                question: "Omi का favorite food क्या है?",
                options: ["समोसा", "डोसा", "इडली", "डोसा"],
                correct: 0
            },
            {
                question: "किस रंग की तरंग सबसे लंबी होती है?",
                options: ["नीला", "पीला", "लाल", "हरा"],
                correct: 2
            }
        ];

        // ==================== INITIALIZATION ====================
        document.addEventListener('DOMContentLoaded', function() {
            loadAppState();
            setupEventListeners();
            setTimestamp();
        });

        function setupEventListeners() {
            // Header buttons
            document.getElementById('settingsBtn').addEventListener('click', () => openModal('settingsModal'));

            // Action buttons
            document.getElementById('chatBtn').addEventListener('click', () => focusChat());
            document.getElementById('roastBtn').addEventListener('click', () => openModal('roastModal'));
            document.getElementById('quizBtn').addEventListener('click', () => openModal('quizModal'));
            document.getElementById('coinsBtn').addEventListener('click', () => openModal('coinsModal'));
            document.getElementById('timePassBtn').addEventListener('click', () => openModal('timePassModal'));
            document.getElementById('historyBtn').addEventListener('click', () => openModal('historyModal'));

            // Chat input
            const chatInput = document.getElementById('chatInput');
            const sendBtn = document.getElementById('sendBtn');

            chatInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter' && !e.shiftKey) {
                    e.preventDefault();
                    sendMessage();
                }
            });

            chatInput.addEventListener('input', function() {
                this.style.height = 'auto';
                this.style.height = Math.min(this.scrollHeight, 100) + 'px';
            });

            sendBtn.addEventListener('click', sendMessage);

            // Roast button
            document.getElementById('roastMeBtn').addEventListener('click', getRoast);

            // Quiz
            document.getElementById('startQuizBtn').addEventListener('click', startQuiz);

            // Coins
            document.getElementById('claimCoinsBtn').addEventListener('click', claimDailyBonus);

            // Time Pass
            document.getElementById('getFactBtn').addEventListener('click', getFact);

            // Settings
            document.getElementById('notificationToggle').addEventListener('change', (e) => {
                appState.notifications = e.target.checked;
                saveAppState();
            });

            document.getElementById('soundToggle').addEventListener('change', (e) => {
                appState.sound = e.target.checked;
                saveAppState();
            });

            document.getElementById('hinglishSlider').addEventListener('change', (e) => {
                appState.hinglishLevel = parseInt(e.target.value);
                updateHinglishLevel();
                saveAppState();
            });

            document.getElementById('clearHistoryBtn').addEventListener('click', clearAllHistory);

            // Close modals on background click
            document.querySelectorAll('.modal').forEach(modal => {
                modal.addEventListener('click', (e) => {
                    if (e.target === modal) {
                        closeModal(modal.id);
                    }
                });
            });
        }

        // ==================== MODAL FUNCTIONS ====================
        function openModal(modalId) {
            const modal = document.getElementById(modalId);
            if (modal) {
                modal.classList.add('active');
                if (modalId === 'historyModal') displayHistory();
                if (modalId === 'coinsModal') updateCoinsDisplay();
            }
        }

        function closeModal(modalId) {
            const modal = document.getElementById(modalId);
            if (modal) {
                modal.classList.remove('active');
            }
        }

        // ==================== CHAT FUNCTIONS ====================
        function sendMessage() {
            const input = document.getElementById('chatInput');
            const message = input.value.trim();

            if (!message) return;

            // Add user message
            addMessage(message, 'user');
            input.value = '';
            input.style.height = 'auto';

            // Add coins for chatting
            appState.samosaCoins += 5;

            // Show typing indicator
            showTypingIndicator();

            // Simulate bot response delay
            setTimeout(() => {
                removeTypingIndicator();
                const response = generateResponse(message);
                addMessage(response, 'bot');

                // Add to history
                appState.chatHistory.push({
                    user: message,
                    bot: response,
                    timestamp: new Date().toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit' })
                });

                saveAppState();
            }, 800 + Math.random() * 1200);
        }

        function generateResponse(userMessage) {
            const personalityLevel = appState.hinglishLevel;
            const personality = hinglishPersonalities[personalityLevel];

            const responses = personality.responses;
            const randomResponse = responses[Math.floor(Math.random() * responses.length)];

            // Analyze message and give contextual response
            const lowerMessage = userMessage.toLowerCase();

            if (lowerMessage.includes('hello') || lowerMessage.includes('hi')) {
                return personality.greeting;
            } else if (lowerMessage.includes('thanks') || lowerMessage.includes('thank you')) {
                return "Welcome bhai! 🙏 Anytime tere liye available hoon!";
            } else if (lowerMessage.includes('name')) {
                return "Main Omi hoon, tера AI companion! 🚀 Best friend mode ON! 😎";
            } else if (lowerMessage.includes('bye') || lowerMessage.includes('goodbye')) {
                return "Phir milenge jaldi! Take care bhai! 👋 OMVERSE mein welcome! 🎉";
            }

            return randomResponse;
        }

        function addMessage(text, sender) {
            const chatArea = document.getElementById('chatArea');

            // Remove empty state if exists
            const emptyState = chatArea.querySelector('.empty-state');
            if (emptyState) {
                emptyState.remove();
            }

            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${sender}`;

            const timeSpan = document.createElement('span');
            timeSpan.className = 'message-time';
            timeSpan.textContent = new Date().toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit' });

            const contentDiv = document.createElement('div');
            contentDiv.className = 'message-content';
            contentDiv.textContent = text;

            messageDiv.appendChild(contentDiv);
            messageDiv.appendChild(timeSpan);

            chatArea.appendChild(messageDiv);
            chatArea.scrollTop = chatArea.scrollHeight;
        }

        function showTypingIndicator() {
            const chatArea = document.getElementById('chatArea');

            const messageDiv = document.createElement('div');
            messageDiv.className = 'message bot';
            messageDiv.id = 'typingIndicator';

            const typingDiv = document.createElement('div');
            typingDiv.className = 'typing-indicator';

            for (let i = 0; i < 3; i++) {
                const dot = document.createElement('div');
                dot.className = 'typing-dot';
                typingDiv.appendChild(dot);
            }

            messageDiv.appendChild(typingDiv);
            chatArea.appendChild(messageDiv);
            chatArea.scrollTop = chatArea.scrollHeight;
        }

        function removeTypingIndicator() {
            const indicator = document.getElementById('typingIndicator');
            if (indicator) indicator.remove();
        }

        function focusChat() {
            document.getElementById('chatInput').focus();
        }

        // ==================== ROAST FUNCTION ====================
        function getRoast() {
            const roastContent = document.getElementById('roastContent');
            const randomRoast = roasts[Math.floor(Math.random() * roasts.length)];
            roastContent.textContent = randomRoast;
            appState.samosaCoins += 10;
            saveAppState();
        }

        // ==================== QUIZ FUNCTIONS ====================
        let currentQuizIndex = 0;
        let quizCorrect = 0;

        function startQuiz() {
            currentQuizIndex = 0;
            quizCorrect = 0;
            showQuizQuestion();
        }

        function showQuizQuestion() {
            if (currentQuizIndex >= quizQuestions.length) {
                showQuizResult();
                return;
            }

            const question = quizQuestions[currentQuizIndex];
            const quizContent = document.getElementById('quizContent');

            let html = `<div class="quiz-question">${question.question}</div>`;
            html += `<div class="quiz-options">`;

            question.options.forEach((option, index) => {
                html += `<button class="quiz-option" onclick="answerQuiz(${index})">${option}</button>`;
            });

            html += `</div>`;

            quizContent.innerHTML = html;
        }

        function answerQuiz(selectedIndex) {
            const question = quizQuestions[currentQuizIndex];

            if (selectedIndex === question.correct) {
                quizCorrect++;
                appState.samosaCoins += 20;
            }

            currentQuizIndex++;
            showQuizQuestion();
        }

        function showQuizResult() {
            const quizContent = document.getElementById('quizContent');
            const percentage = (quizCorrect / quizQuestions.length) * 100;

            let resultText = `बहुत अच्छा! 🎉`;
            if (percentage === 100) resultText = `Perfect score! तू genius hai! 🧠✨`;
            else if (percentage >= 80) resultText = `Brilliant! तू awesome है! 🚀`;
            else if (percentage >= 60) resultText = `अच्छा प्रयास! अगली बार और अच्छा करना! 💪`;

            quizContent.innerHTML = `
                <div class="quiz-score">${quizCorrect}/${quizQuestions.length}</div>
                <div style="text-align: center; color: var(--text-secondary); margin-bottom: 16px;">
                    <p style="font-size: 16px; margin-bottom: 8px;">${resultText}</p>
                    <p>Score: ${percentage.toFixed(0)}%</p>
                </div>
                <button class="time-pass-btn" onclick="startQuiz()">Play Again</button>
            `;

            appState.quizScore = Math.max(appState.quizScore, quizCorrect);
            saveAppState();
        }

        // ==================== COINS FUNCTIONS ====================
        function updateCoinsDisplay() {
            document.getElementById('coinsAmount').textContent = appState.samosaCoins;
        }

        function claimDailyBonus() {
            const today = new Date().toDateString();

            if (appState.lastDailyBonus === today) {
                showNotification('आप आज का बोनस पहले ही ले चुके हैं! कल फिर आना! 😊');
                return;
            }

            appState.samosaCoins += 50;
            appState.lastDailyBonus = today;
            saveAppState();

            updateCoinsDisplay();
            showNotification('🥟 50 Samosa Coins का बोनस मिल गया! 🎉');
        }

        // ==================== TIME PASS FUNCTIONS ====================
        function getFact() {
            const timePassContent = document.getElementById('timePassContent');
            const randomFact = facts[Math.floor(Math.random() * facts.length)];

            timePassContent.innerHTML = `
                <div class="fact-card">${randomFact}</div>
                <button class="time-pass-btn" onclick="getFact()" style="margin-top: 12px;">अगला Fact देखो</button>
            `;

            appState.samosaCoins += 3;
            saveAppState();
        }

        // ==================== HISTORY FUNCTIONS ====================
        function displayHistory() {
            const historyContent = document.getElementById('historyContent');

            if (appState.chatHistory.length === 0) {
                historyContent.innerHTML = `
                    <div class="empty-state" style="height: auto; padding: 20px;">
                        <div class="empty-state-text">No chat history yet. Start chatting with Omi! 💬</div>
                    </div>
                `;
                return;
            }

            let html = '<div style="display: flex; flex-direction: column; gap: 12px;">';

            appState.chatHistory.forEach((item, index) => {
                html += `
                    <div style="background: rgba(26, 31, 58, 0.4); padding: 12px; border-radius: 10px; border-left: 3px solid var(--primary);">
                        <div style="color: var(--primary); font-weight: 600; margin-bottom: 4px; font-size: 12px;">You - ${item.timestamp}</div>
                        <div style="color: var(--text-secondary); font-size: 13px; margin-bottom: 8px;">${item.user}</div>
                        <div style="color: var(--text-primary); font-size: 13px;">Omi: ${item.bot}</div>
                    </div>
                `;
            });

            html += '</div>';
            historyContent.innerHTML = html;
        }

        function clearAllHistory() {
            if (confirm('क्या आप सभी chat history को delete करना चाहते हो? यह action reversible नहीं है! 🗑️')) {
                appState.chatHistory = [];
                saveAppState();
                displayHistory();
                showNotification('Chat history clear हो गया! 🗑️');
            }
        }

        // ==================== SETTINGS ====================
        function updateHinglishLevel() {
            const levels = ['Light', 'Medium', 'Strong'];
            document.getElementById('hinglishLevel').textContent = levels[appState.hinglishLevel - 1];
        }

        // ==================== LOCAL STORAGE ====================
        function saveAppState() {
            localStorage.setItem('omverseAppState', JSON.stringify(appState));
        }

        function loadAppState() {
            const saved = localStorage.getItem('omverseAppState');
            if (saved) {
                Object.assign(appState, JSON.parse(saved));
                updateHinglishLevel();
                document.getElementById('hinglishSlider').value = appState.hinglishLevel;
                document.getElementById('notificationToggle').checked = appState.notifications;
                document.getElementById('soundToggle').checked = appState.sound;
            }
        }

        // ==================== UTILITIES ====================
        function showNotification(message) {
            if (!appState.notifications) return;

            const notification = document.createElement('div');
            notification.className = 'notification';
            notification.textContent = message;

            document.body.appendChild(notification);

            setTimeout(() => {
                notification.style.animation = 'slideUp 0.3s ease reverse';
                setTimeout(() => notification.remove(), 300);
            }, 3000);
        }

        function setTimestamp() {
            const timeElement = document.querySelector('.status-text');
            setInterval(() => {
                const hour = new Date().getHours();
                let status = '● Online';
                if (hour >= 23 || hour < 6) status = '🌙 Night Owl Mode';
                else if (hour >= 12 && hour < 17) status = '☀️ Afternoon Chat';
                timeElement.textContent = status;
            }, 60000);
        }

        // ==================== INITIAL GREETING ====================
        window.addEventListener('load', function() {
            setTimeout(() => {
                const personality = hinglishPersonalities[appState.hinglishLevel];
                addMessage(personality.greeting, 'bot');
            }, 500);
        });
    </script>
</body>
  </html>
